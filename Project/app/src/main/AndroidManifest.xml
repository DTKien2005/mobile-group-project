<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.covid19app">

    <!-- Allow app connect Internet-->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Covid19App"
        tools:replace="android:icon,android:supportsRtl">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.CountryBrowserActivity" />

        <activity
            android:name=".features.vndashboard.ui.view.VnDashboardActivity"
            android:exported="false" />
        <activity android:name=".D30Activity"
            android:exported="false"/>
    </application>

</manifest>